name: TEMP Cleanup 15F Project Analysis finalizer

on:
  push:
    branches:
      - cleanup/ai-project-analysis-20260814
    paths:
      - scripts/finalize-ai-migration-15f-project-analysis.mjs
      - .github/workflows/temp-ai-finalize-15f-project-analysis.yml

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    timeout-minutes: 7
    steps:
      - uses: actions/checkout@v4
        with:
          ref: cleanup/ai-project-analysis-20260814
          fetch-depth: 1
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Apply Project Analysis finalizer
        run: node scripts/finalize-ai-migration-15f-project-analysis.mjs
      - name: Validate final migration contracts
        run: |
          node scripts/project-analysis-ai-smoke.mjs
          node scripts/q-analyze-batch-ai-smoke.mjs
          node scripts/q-analyze-offer-ai-smoke.mjs
          node scripts/parse-offer-ai-smoke.mjs
          node scripts/start-parsing-ai-smoke.mjs
          node scripts/ai-provider-contract-check.mjs
          node scripts/ai-runtime-callsite-inventory.mjs
          node scripts/runtime-manifest-check.mjs
          node scripts/bootstrap-sequence-check.mjs
          node scripts/generate-bootstrap.mjs --check
          git diff --check
      - name: Commit permanent finalization
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/ai-runtime-callsite-inventory.mjs scripts/ai-provider-contract-check.mjs docs/AI_PROVIDER_RUNTIME.md
          if git diff --cached --quiet; then
            echo "No finalization changes produced." >&2
            exit 1
          fi
          git commit -m "Finalize Project Analysis AI migration guards"
          git push origin HEAD:cleanup/ai-project-analysis-20260814
