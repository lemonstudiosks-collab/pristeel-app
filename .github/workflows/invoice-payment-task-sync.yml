name: Invoice payment task sync

on:
  workflow_dispatch:
    inputs:
      mode:
        description: Sync mode
        required: true
        default: preview
        type: choice
        options:
          - preview
          - apply
  schedule:
    - cron: '47 6 * * *'

permissions:
  contents: read

concurrency:
  group: invoice-payment-task-sync
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL || 'https://isymxqfqzkchbsrbhucf.supabase.co' }}
      SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY || secrets.SUPABASE_SERVICE_KEY }}
      PPPP_SYNC_EMAIL: ${{ secrets.PPPP_SYNC_EMAIL }}
      PPPP_SYNC_PASSWORD: ${{ secrets.PPPP_SYNC_PASSWORD }}
      PAYMENT_TASK_LOOKAHEAD_DAYS: '7'
      SYNC_MODE: ${{ github.event_name == 'schedule' && 'apply' || inputs.mode }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Detect Supabase credentials
        id: credential
        shell: bash
        run: |
          if { [ -n "$SUPABASE_SECRET_KEY" ] || [ -n "$SUPABASE_SERVICE_ROLE_KEY" ]; } || { [ -n "$PPPP_SYNC_EMAIL" ] && [ -n "$PPPP_SYNC_PASSWORD" ]; }; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "::notice::No Supabase service key or PPPP sync account is configured."
          fi

      - name: Synchronize invoice payment tasks
        if: steps.credential.outputs.available == 'true'
        run: node scripts/invoice-payment-task-sync.mjs

      - name: Upload sanitized summary
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: invoice-payment-task-sync
          path: tmp/invoice-payment-task-sync.json
          if-no-files-found: ignore
          retention-days: 7
