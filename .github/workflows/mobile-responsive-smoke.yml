name: Mobile Responsive Smoke

on:
  pull_request:
    branches: [main]
  push:
    branches:
      - 'chatgpt/mobile-responsive-v1'

permissions:
  contents: read

jobs:
  mobile-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Check mobile runtime syntax
        run: |\n          node --check pristeel-mobile-responsive-v1.js\n          node --check pristeel-mobile-home-v1.js
      - name: Verify mobile shell contract
        run: |\n          node tests/mobile-responsive-shell-smoke.mjs\n          node tests/mobile-home-smoke.mjs
