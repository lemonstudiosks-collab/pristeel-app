name: TEMP Cleanup 15E3 qAnalyzeOffer finalizer

on:
  push:
    branches:
      - cleanup/ai-inline-qanalyzeoffer-20260814
    paths:
      - scripts/finalize-ai-migration-15e3-qanalyzeoffer.mjs
      - .github/workflows/temp-ai-finalize-15e3-qanalyzeoffer.yml

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    timeout-minutes: 7
    steps:
      - uses: actions/checkout@v4
        with:
          ref: cleanup/ai-inline-qanalyzeoffer-20260814
          fetch-depth: 1
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Apply qAnalyzeOffer finalizer
        run: node scripts/finalize-ai-migration-15e3-qanalyzeoffer.mjs
      - name: Validate final migration contracts
        run: |
          node scripts/q-analyze-offer-ai-smoke.mjs
          node scripts/parse-offer-ai-smoke.mjs
          node scripts/start-parsing-ai-smoke.mjs
          node scripts/ai-provider-contract-check.mjs
          node scripts/ai-runtime-callsite-inventory.mjs
          node scripts/runtime-manifest-check.mjs
          node scripts/bootstrap-sequence-check.mjs
          node scripts/generate-bootstrap.mjs --check
          git diff --check
      - name: Commit permanent finalization
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/ai-runtime-callsite-inventory.mjs scripts/ai-provider-contract-check.mjs docs/AI_PROVIDER_RUNTIME.md .github/workflows/pristeel-tests.yml
          if git diff --cached --quiet; then
            echo "No finalization changes produced." >&2
            exit 1
          fi
          git commit -m "Finalize qAnalyzeOffer AI migration guards"
          git push origin HEAD:cleanup/ai-inline-qanalyzeoffer-20260814
