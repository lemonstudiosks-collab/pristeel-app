const fs=require('fs');const assert=require('assert');const s=fs.readFileSync('pristeel-commercial-navigation-fix-v1.js','utf8');assert(s.includes("typeof window.oaNew==='function'"),'Offer tile must open a new client offer');assert(s.includes("target.closest('[data-type]')"),'Document type tiles must be actionable');assert(s.includes("target.closest('#pst-dc-new')"),'New document button must be actionable');assert(s.includes("oaOpenQuoteModal"),'Existing offers must open from the register');console.log('Document center actions smoke test passed.');