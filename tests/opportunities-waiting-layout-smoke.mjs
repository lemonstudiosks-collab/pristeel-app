import fs from 'node:fs';
import assert from 'node:assert/strict';

const bridge=fs.readFileSync('pristeel-opportunities-waiting-bridge-v1.js','utf8');
const desk=fs.readFileSync('pristeel-opportunities-filter-polish-v1.js','utf8');
const interaction=fs.readFileSync('pristeel-home-canonical-interaction-v1.js','utf8');
const workflow=fs.readFileSync('pristeel-project-centric-workflow-v1.js','utf8');

assert.doesNotThrow(()=>new Function(desk),'Opportunities Desk must be valid JavaScript');
assert.doesNotThrow(()=>new Function(workflow),'Project-Centric workflow must remain valid JavaScript');
assert.match(bridge,/next==='draft'\?'waiting':next/,'legacy draft lifecycle must normalize to waiting');
assert.match(desk,/VERSION='20260923-layout-stability1'/,'Opportunity Desk revision must be active');
assert.match(desk,/data-pst-opp-mode/,'Desk must expose the direct-tender versus TED-award work routes');
assert.match(desk,/data-pst-opp-source/,'source filters must remain functional');
assert.match(desk,/resultPage/,'category selection must have a dedicated result-page state');
assert.match(desk,/pst-opp-dashboard/,'initial Opportunities view must have an explicit dashboard-only state');
assert.match(desk,/pst-opp-result-page/,'category selection must render as a dedicated result page instead of inline results');
assert.match(desk,/openResultPage/,'all dashboard categories must route through one result-page navigation owner');
assert.match(desk,/pst-opp-dashboard #pst-opportunities-list/,'the canonical result list must be hidden on the dashboard');
assert.match(desk,/data-pst-opp-field/,'field filters must remain functional');
assert.match(desk,/data-pst-opp-winner/,'TED winner-role filters must remain functional');
assert.match(desk,/data-pst-opp-lifecycle/,'status filters must remain functional');
assert.match(desk,/Për ofertim/,'Direct Tender route must be operator-readable');
assert.match(desk,/sectionLabel\('◷','Statusi'/,'Statusi must be rendered as a strong dashboard section heading');
assert.match(desk,/sectionLabel\('▤','Burimi'/,'Burimi must be rendered as a strong dashboard section heading');
assert.match(desk,/sectionLabel\('▦','Fusha'/,'Fusha must be rendered as a strong dashboard section heading');
assert.match(desk,/sectionLabel\('♜','Fituesi TED'/,'Fituesi TED must be rendered as a strong dashboard section heading');
assert.doesNotMatch(desk,/data-pst-opp-lifecycle','all'/,'Statusi must not render a duplicate all control');
assert.doesNotMatch(desk,/data-pst-opp-source','all'/,'Burimi must not render a duplicate all control');
assert.doesNotMatch(desk,/data-pst-opp-field','all'/,'Fusha must not render a duplicate all control');
assert.doesNotMatch(desk,/data-pst-opp-winner','all'/,'Fituesi TED must not render a duplicate all control');
assert.match(desk,/Fitues për kontaktim/,'TED award-sales route must be operator-readable');
assert.doesNotMatch(desk,/pst-opp-mini-stats/,'passive summary cards must be removed from the Opportunity Desk');
assert.match(desk,/\.pst-opp-desk-back\{[^}]*background:#3f9fc2[^}]*color:#fff!important/s,'Kthehu must use the blue primary treatment with white text');
assert.doesNotMatch(desk,/data-pst-opp-source="UNDP_KOSOVO"/,'UNDP Kosovo must not be rendered as a source chip');
assert.match(desk,/Number\(c\.src\[k\]\|\|0\)>0/,'zero-count sources must be omitted from the source filter surface');
assert.match(workflow,/display_limit:40/,'results must use bounded progressive disclosure');
assert.doesNotMatch(workflow,/return rows\.slice\(0,80\)/,'canonical result set must not silently discard rows after 80');
assert.match(workflow,/function showMoreOpportunities\(/,'workflow must expose progressive load-more behavior');
assert.match(workflow,/kind==='mode'/,'route mode must be a canonical filter');
assert.doesNotMatch(workflow,/tenderState\.source='all';tenderState\.field='all';tenderState\.winner_group='all';\s*var life/,'status filtering must no longer wipe other filters');
assert.match(interaction,/pristeel-opportunities-filter-polish-v1\.js\?v=20260923-layout-stability1/,'runtime must cache-bust the new Opportunity Desk');
console.log('Opportunities Desk layout smoke: OK');
