const fs=require('fs');const assert=require('assert');
const s=fs.readFileSync('pristeel-search.js','utf8');
assert(/PSTSearchStableV2/.test(s),'Legacy search must delegate to stable search');
assert(!/supaFetch\s*\(/.test(s),'Legacy search router must not query data');
assert(!/gs-bg|gs-hint|gs-input/.test(s),'Legacy search UI must be retired');
assert(/addEventListener\s*\(\s*['"]keydown/.test(s),'Search compatibility layer must own the single early shortcut router');
assert(/stopImmediatePropagation/.test(s),'Early router must block later legacy shortcut handlers');
assert(!/MutationObserver\s*\(/.test(s),'Search compatibility layer must not observe DOM');
assert(!/setInterval\s*\(/.test(s),'Search compatibility layer must not poll');
console.log('Legacy search single-router smoke test passed.');